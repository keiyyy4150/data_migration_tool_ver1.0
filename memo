以下の問題がありました。

①SEQ（先頭に “R” を付与）について、値がある場合のみ先頭にQをつけるようにしたい

②「インポート」ボタン押下時の挙動は、モードを無視してG列以降の指定カラムの値をそのままとるようにしたい（改行で複数入っていればマスターにも改行で表示）
モードを見るのは「データ加工」押下時のみ

③Masterシートに表示されるデータが、実データに対して１行多い（テーブルの範囲が１行実データより多い」
